---
from: mini
to: jesse
date: 2026-04-18
subject: RE: Recovery audit — full response on all 7 points
priority: high
---

Caught up. Your message arrived on the 20:07 git push but my watcher only scanned for `r1vs-*-finalized.md` — `jesse-to-mini-*` files were invisible to it. Fixed this cycle; watcher now scans both patterns. Won't happen again.

---

## 1. Protocol refinements not yet codified

Two gaps from the dead R1VS session chat relay:

**Q1 from R1VS (0845 message, never answered):** Sites with only 1-2 verbatim reviews (handy-dandy-atlanta, plugged-electricians-atl) — skip the marquee, use the 1-2 reviews as STATIC pull quotes only. No marquee if <3 reviews to scroll. This is now in SKILL.md ("Sites with only 1-2 verbatim reviews: use as static pull quotes only, skip the marquee/feed component"). R1VS's 0845 asked but never got an explicit ACK from us. Consider this the ACK.

**Marquee speed convergence:** SKILL.md now says 58s brand marquee / 60s reviewScroll (converged from the batch). The old "55-60s range" guidance is superseded. SKILL.md is correct.

**Nothing else uncodified** that I can see from the message history. The 6 polish rules, component CSS specs, stats de-dup rule, and footer mobile pattern all landed in SKILL.md via this morning's push. Division of labor, handoff filename contracts, and git protocol are all in CLAUDE.md / R1VS-REBUILD-BRIEF.md.

**One admin note:** The SKILL.md commit `0c7e1b3` you mentioned (worktree branch `claude/silly-mclean-e07792`) — the content IS already on main. The pull at the top of this cron cycle confirmed SKILL.md is current. You can skip the cherry-pick; it's already merged.

---

## 2. Supabase schema snapshot

**Project ID:** `qztjoshdrxionhxeieik`

⚠️ `supabase/schema.sql` in brucecom-v3 is STALE — it shows old stage values. The migrations are authoritative.

### `public.prospects` — main CRM table

Key columns R1VS needs:
- `slug` (text, unique) — must match intake branch slug exactly
- `stage` (text) — current pipeline position
- `claim_code` (text) — generated by Mini post-intake-accept
- `preview_site_url` (text) — set by Mini after deploy
- `has_address`, `has_email`, `has_phone` — GENERATED columns, never INSERT

**Stage machine (in order, authoritative as of migration 20260412):**
```
research → site_built → ready_for_review → qa_approved → outreach_staged → outreach_sent → converted → dead
```
Killed stages (no longer valid): `claude_reviewed`, `design_review`, `in_window`, `follow_up`, `disqualified`. These were migrated away in 20260412_simplify_pipeline_stages.sql.

Also valid (edge states): `stuck` (added 20260404)

### `public.site_intake` — R1VS handoff table (brief-9)

This is the table R1VS POSTs to. State machine:
```
received → reviewing → accepted → revision_needed → rejected → built → deployed
```
Key columns:
- `slug` (text, unique) — intake identifier
- `state` (text) — lifecycle position (NOT the same as `prospects.stage`)
- `proposal_sha` (text) — idempotency key; (slug, proposal_sha) is unique
- `git_branch` (text) — e.g. `intake/affordable-concrete-repair`
- `prospect_id` (uuid FK) — linked after Mini accepts + creates/matches the prospect row
- `state_code` (text) — 2-letter US state (renamed from `state` to avoid collision)

### Other tables (reference only, R1VS doesn't write to these)
- `public.notes` — QA flags. `feedback_status` values: `open` → `in_progress` → `fixed` → `verified`
- `public.prospect_photos` — selected GBP photos per prospect
- `public.outreach_events` — email/postcard event tracking
- `public.activity_log` — audit trail
- `public.email_log` — inbound support emails

---

## 3. Claim bar shared template

**Path (Mini side):** `gtmdot/sites/_shared/claim-ui.html`
**Injected by:** `brucecom-v3/scripts/process-intake.sh` (step 4/7) and `scripts/bulk-inject-claim-ui.ts` (batch retrofitter)
**Current SHA:** `e59f489` ("feat(brief-7): retrofit claim bar + popup across 36 non-live sites")

**Template variables:**
| Variable | Required | Default | Source |
|---|---|---|---|
| `{{ CLAIM_CODE }}` | Yes | — | `prospects.claim_code` |
| `{{ BUSINESS_NAME }}` | Yes | — | `prospects.business_name` |
| `{{ PRICE_FIRST_MONTH }}` | No | `$49` | hardcoded default |
| `{{ PRICE_ONGOING }}` | No | `$149` | hardcoded default |
| `{{ CHECKOUT_URL }}` | No | `https://gtmdot.com/checkout?code={{ CLAIM_CODE }}` | generated |
| `{{ HOW_IT_WORKS_URL }}` | No | `https://gtmdot.com/how-it-works` | hardcoded default |

**CSS namespace:** `.gtmdot-claim-*` — won't collide with R1VS-built sites.

**Important for R1VS builds:** The template includes a `display: none !important` suppressor block for ALL known legacy bar/popup/cookie patterns: `#claimBar`, `#claim-bar`, `#exitPopup`, `#exit-popup`, `#cookieBanner`, etc. This is intentional. R1VS should NOT build its own claim bar or popup — the injector will handle it. R1VS should NOT add `<!-- CLAIM_BAR_ANCHOR -->` either — Mini finds `</body>` and injects before it.

---

## 4. Pipeline state — ~20 remaining un-polished intake branches

Polish status: 5 sites have the 6 polish rules applied (sandy-springs, moonstone, perez-pools, pine-peach, atlanta-expert). The others don't. All sites below have r1vs-finalization messages.

| Slug | `prospects.stage` | Bruce enrichment | Known blockers |
|---|---|---|---|
| 24-hrs-mobile-tire-services | `ready_for_review` | none | needs Bruce cycle |
| affordable-concrete-repair | `outreach_sent` | none | needs Bruce cycle |
| azer-pool | `outreach_staged` | none | needs Bruce cycle |
| bobs-hvac | (not in prospects yet) | none | intake-only so far |
| cityboys | `outreach_sent` | none | needs Bruce cycle |
| dream-steam | `outreach_staged` | none | needs Bruce cycle |
| handy-dandy-atlanta | `outreach_staged` | none | only 1-2 reviews → static pull quotes only, no marquee |
| intire-mobile-tire-shop | `outreach_sent` | none | needs Bruce cycle |
| locksmith-atlanta-pro | `outreach_sent` | none | needs Bruce cycle |
| membrenos-pro-home-repair | `outreach_sent` | none | needs Bruce cycle |
| moonstone-pressure-washing | `outreach_sent` | none | polished ✅ — deploy blocked on outreach hold |
| perez-pools-llc | `outreach_sent` | none | polished ✅ — deploy blocked on outreach hold |
| pine-peach-painting | `ready_for_review` | none (grandfathered) | polished ✅ |
| plugged-electricians-atl | `outreach_sent` | none | only 1-2 reviews → static pull quotes only, no marquee |
| professional-gutter-cleaning | `ready_for_review` | none | needs Bruce cycle (note: distinct from `pro-gutter-cleaning`) |
| tech-on-the-way | `outreach_sent` | none | needs Bruce cycle |
| the-appliance-gals | `qa_approved` | none | no gate (grandfathered) |
| tire-and-ride-mobile | `outreach_sent` | none | needs Bruce cycle |
| tuckers-home-services | `outreach_sent` | none | needs Bruce cycle |

**Grandfathered sites** (already deployed, no Bruce gate needed):
atlanta-expert-appliance (polished ✅), atlanta-drywall-1, bravo-plumbing-solutions, chrissy-s-mobile-detailing, doctor-concrete-atl, es-tree-service, forest-park-collision, hvac-guyz-plumbing-inc, jack-glass-electric, pine-peach-painting (polished ✅), premier-tv-mounting-atl, pro-gutter-cleaning, rooter-pro-plumbing-drain, sandy-springs-plumbing (polished ✅), trushyne-mobile-detailing

**Prioritization suggestion for R1VS retrofit order:** Polished sites first so they're ready the moment the outreach hold lifts. Then prioritize `outreach_sent` stage (10 sites — were active in sequences). Then `outreach_staged` (3). Then `ready_for_review` that aren't grandfathered (2).

---

## 5. Bruce status

**Currently NOT active.** Last enrichment batch was Apr 17 at 20:18 (13 sites). No enriched or blocked messages since then.

**4 sites with BLOCKED enrichment:**
- `atlanta-expert-appliance` — blocked (grandfathered, already deployed anyway)
- `doctor-concrete-atl` — blocked (grandfathered)
- `premier-tv-mounting-atl` — blocked (grandfathered)
- `pro-gutter-cleaning` — blocked (grandfathered)

Blocked reason details are in each `bruce-to-mini-<slug>-blocked.md` file in messages/. These are all grandfathered, so R1VS can treat their intake branches as normal — Mini bypassed the gate for them at deploy time.

**Photo waterfall rules:** No changes since the 1710 correction message (Apr 17). Rule stands: Bruce delivers photos via the bruce skill; R1VS wires them in and removes photo-brief.json. Don't use Unsplash stock in gallery slots — only GBP/Recraft.

---

## 6. Environment variables

`~/.openclaw/.env` on Mini side — **confirmed exists**, last modified Mar 29. Contains:

✅ `GOOGLE_MAPS_API_KEY` — present  
✅ `FIRECRAWL_API_KEY` — present  
✅ `RECRAFT_API_KEY` — present  
✅ `POPLAR_API_KEY` — present  

**R1VS MacBook side:** SKILL.md says keys live at `~/.openclaw/.env` on both machines. The MacBook instance needs its own copy. I can't verify the MacBook's env directly, but the SKILL.md path (`~/.openclaw/.env`) is the canonical location for R1VS to read from.

**`INTAKE_BEARER_TOKEN` note:** This key is NOT in Mini's `~/.openclaw/.env` — it's not needed on the Mini side (Mini verifies inbound requests using its own `SUPABASE_SERVICE_KEY`). The `INTAKE_BEARER_TOKEN` is a secret that R1VS sends in the Authorization header when POSTing to `crm.cloakanddagger.co/api/site-intake`. The value is set via `INTAKE_BEARER_TOKEN` in brucecom-v3's `.env.local` (Mini side, server-side verification). R1VS needs to know the token value — Jesse, if you haven't already given R1VS the token for the fresh session, that's the one thing to pass at session start.

---

## 7. Anything else

One thing not covered elsewhere: **the outreach hard hold is still active.** Brief 15 hasn't shipped yet and Jesse hasn't done a visual approval pass. All email sends are frozen. This means sites at `outreach_staged` / `outreach_sent` stages won't get new sequence triggers until the hold lifts. R1VS should know: retrofitting + deploying is fine; the hold is on send actions only, not builds.

The `bobs-hvac` slug has a finalization message but no prospect row in Supabase yet. Mini hasn't processed it. When R1VS is ready to have it deployed, it's in the queue — it'll get a prospect row created at intake-accept time.

No other gaps identified.

---

Ready when you are to start the fresh R1VS session. Flag if anything above needs correction.

Mini
